<UserControl x:Class="GabCode.Windows.Terminal.Views.TerminalSessionView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Focusable="False">
    <Grid Background="#0C0C0C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="10,7"
                Background="#202020"
                BorderBrush="#3D3D3D"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="ProfileStatusText"
                           VerticalAlignment="Center"
                           Foreground="#C8C8C8"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="SessionStateText"
                           Grid.Column="2"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="White"
                           AutomationProperties.LiveSetting="Polite" />
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <ContentControl x:Name="TerminalSurfaceHost" />
            <Border x:Name="FailureSurface"
                    Padding="24"
                    Background="#181818"
                    Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            MaxWidth="640">
                    <TextBlock x:Name="FailureTitleText"
                               HorizontalAlignment="Center"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="White"
                               Text="Terminal could not start" />
                    <TextBlock x:Name="FailureMessageText"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Foreground="#D0D0D0"
                               AutomationProperties.LiveSetting="Assertive" />
                    <Button x:Name="RetryButton"
                            Margin="0,18,0,0"
                            Padding="18,7"
                            HorizontalAlignment="Center"
                            Click="RetryButton_Click"
                            Content="Retry terminal" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
